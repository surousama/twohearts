# 角色移动系统实施计划

## 方案：Lyra 方案（Distance Matching + Stride Warping）

## 阶段一：补齐基础设施

### 1. 启用插件
- [x] AnimationWarping（已启用）
- [x] AnimationLocomotionLibrary（已添加到 .uproject）

### 2. 为重定向动画生成 Distance Curve
- 批量选中 Chinese_Warrior/Animations 下的 Start/Stop 动画
- 右键 → Animation Modifier → 添加 `DistanceCurveModifier`
- 曲线名设为 `Distance`

### 3. 为动画添加脚步同步标记
- 对 Start/Cycle 动画添加 `SyncMarkerAnimModifier`
- 自动在脚步触地点生成同步标记

### 4. 创建 Blend Space 2D
- [x] `BS_Walk`（Speed=200 × Direction）—— Walk Loop 动画（8方向）
- [x] `BS_Run`（Speed=400 × Direction）—— Run Loop 动画（8方向）
- [x] `BS_Sprint`（Speed=500 × Direction）—— Sprint Loop 动画（仅前方3方向）

## 阶段二：搭建新 AnimBP

> 新建 `ABP_ChineseWarrior`（位于 `Content/Chinese_Warrior/Animation/`），不修改旧的 ThirdPerson_AnimBP。
> 等 AnimBP 搭建完成后再切换 BP_ThirdPersonCharacter 的 Anim Class。

### Event Graph（计算层）—— ✅ 已完成

变量列表：

| 变量名 | 类型 | Category | 说明 |
|--------|------|----------|------|
| GroundSpeed | Float | Movement | 水平移动速度（Vector Length XY） |
| DistanceTraveled | Float | Movement | 上一帧移动距离（GroundSpeed × DeltaTime） |
| bShouldMove | Bool | Movement | GroundSpeed > 3.0 AND Acceleration Length > 0 |
| MovementDirection | Float | Movement | 移动方向角度，-180~180（Calculate Direction） |
| PredictedStopDistance | Float | Movement | 预测刹车距离（Speed² / (2 × BrakingDecelerationWalking)） |
| Velocity | Vector | Movement | 缓存角色速度向量 |
| Acceleration | Vector | Movement | 缓存角色加速度向量 |
| DeltaTimeX | Float | Movement | 缓存帧间隔时间 |
| CharacterMovement | CharacterMovementComponent | Movement | 缓存运动组件引用 |

蓝图连线流程：
```
Event Blueprint Update Animation (DeltaTimeX)
  → Cast To Character → Set DeltaTimeX → Set CharacterMovement
  → Set Velocity → Set Acceleration → Set GroundSpeed → Set bShouldMove
  → Set MovementDirection → Set DistanceTraveled → Set PredictedStopDistance
```

### Anim Graph（状态机）

```
Idle → Start → Cycle → Stop → Idle
                ↑                │
                └── Pivot ───────┘
```

| 状态 | 动画节点 | 驱动方式 | 进度 |
|------|---------|---------|------|
| Idle | Sequence Player | 直接播放 Idle_Loop | ✅ 已完成 |
| Start | Sequence Evaluator | Advance Time by Distance Matching | ✅ 已完成（Explicit Time = Always Dynamic） |
| Cycle | Blend Space Player | BS_Run + GroundSpeed/MovementDirection | ✅ 基础版已完成（待优化） |
| Stop | Sequence Evaluator | Distance Match to Target + 分支播放 | ✅ 已完成（含减速匹配+正常播放分支） |
| Pivot | Sequence Evaluator | Distance Match to Target + Advance Time by Distance Matching | ✅ Distance Matching 两阶段驱动已完成（待修复朝向问题） |

#### Cycle 状态待优化项
- [ ] **Walk / Run / Sprint 切换**：当前固定使用 BS_Run，需要根据 GroundSpeed 区间切换 BS_Walk / BS_Run / BS_Sprint
- [ ] **防滑步（Playrate 匹配速度）**：Set Playrate to Match Speed 不支持 Blend Space Player，需要在 Event Graph 中手动计算动态 Play Rate 并绑定

#### Pivot 状态实现（基于 Lyra 实际做法）

**原理**：处理角色移动中突然反方向输入的回转动画（急停 → 转身 → 反向起步）

**触发条件**：
```
(Velocity2D · Acceleration2D) < 0  AND  GroundSpeed > 50
```

**已完成的步骤**：
- [x] Event Graph 新增 bShouldPivot 变量（点积计算）
- [x] Event Graph 新增 PredictedPivotDistance 变量（PredictGroundMovementPivotLocation）
- [x] 添加 Cycle → Pivot 转换线（条件：bShouldPivot == true）
- [x] Pivot → Cycle 转换线（自动规则）
- [x] Pivot 内部用 Sequence Player 跑通流程验证
- [x] 升级为 Sequence Evaluator + Distance Matching 两阶段驱动
- [x] 创建 CC_UniformIndexable（Curve Compression Settings）解决 Distance 曲线压缩问题
- [x] 修复 Branch 条件：从 GroundSpeed > 30 改为 bShouldPivot（速度与加速度是否仍然相反）

**Event Graph 新增变量**：

| 变量名 | 类型 | Category | 说明 |
|--------|------|----------|------|
| bShouldPivot | Bool | Movement | 速度与加速度点积 < 0 且 GroundSpeed > 50 |
| PredictedPivotDistance | Float | Movement | PredictGroundMovementPivotLocation 返回值的 Vector Length |

**OnUpdate 连线方案（Lyra 做法）**：
```
Node → Convert to Sequence Evaluator (Succeeded) → Branch (bShouldPivot)

True（急停阶段，速度与加速度仍然相反，还在靠近回转点）：
  → Distance Match to Target
    - Distance to Target: PredictedPivotDistance
    - Distance Curve Name: "Distance"
  → Return Node

False（反向起步阶段，速度与加速度同向，已过回转点开始反向加速）：
  → Advance Time by Distance Matching
    - Distance Traveled: DistanceTraveled
    - Distance Curve Name: "Distance"
  → Return Node
```

**踩坑记录**：
1. Distance 曲线压缩问题：所有使用 Distance Matching 的动画必须设置 Curve Compression Settings 为 `CC_UniformIndexable`（Codec: UniformIndexable），否则 `FAnimCurveBufferAccess` 报错且打包后失效
2. Branch 条件不能用 GroundSpeed > 30，因为到达回转点时角色可能仍有惯性速度，导致一直走 Distance Match 分支而 PredictedPivotDistance 已经为 0，动画卡死在第一帧
3. 正确做法：用 bShouldPivot（速度·加速度点积）判断，到达回转点后速度与加速度变为同向，自动切换到 Advance Time 推进动画

**待修复——Pivot 朝向问题**：
- [ ] 角色启用了 Orient Rotation to Movement，Pivot 时角色先转向了新方向，然后才播放 Pivot 动作，导致动画方向完全反了
- [ ] 解决方案：Pivot On Become Relevant 中临时禁用 Orient Rotation to Movement，Pivot 结束后恢复
  ```
  On Become Relevant:
    → CharacterMovement → Set Orient Rotation to Movement = false
  
  On Left（或 Cycle/Stop 的 On Become Relevant）:
    → CharacterMovement → Set Orient Rotation to Movement = true
  ```

**后续升级计划**：
- [ ] Pivot A/B 双状态机处理连续反向
- [ ] 根据加速度方向选择对应 Pivot 动画（当前固定用 F_B_Lfoot）
- [ ] 垂直移动检测优化退出条件
- [ ] AnimNotify 通知退出
- [ ] Orientation Warping + Stride Warping 后处理

### 过渡方式
- 所有状态过渡使用 **Inertialization Blend**（惯性混合），替代传统 Cross Fade

### 后处理
- Orientation Warping（朝向适配）
- Stride Warping（步幅适配）
- Foot Placement（地形 IK，已实现）

## 阶段三：逐步完善

- 加入 Sync Group 同步 Start ↔ Cycle 脚步相位
- 处理 Walk ↔ Run ↔ Sprint 速度层切换（Cycle 状态待优化项）
- Cycle 防滑步：手动计算 Play Rate（Cycle 状态待优化项）
- 优化 Pivot 方向判断逻辑（夹角 > 135° 触发）
- Start/Stop 动画根据当前领先脚自动选择 Lfoot/Rfoot 版本
  - 当前 Stop 只用了 Lfoot 版本，右脚着地时松手会连续迈两次右脚
  - 需要检测当前领先脚（通过 Sync Marker 或 foot curve），新增 bLeftFootForward 变量
  - 根据领先脚动态切换 Lfoot/Rfoot 版本的 Stop 动画
- Stop 动画（ChineseWarrior_Run_Stop_F_Lfoot）第 75 帧后为空白内容，Stop → Idle 转换设为提前 1.6 秒触发

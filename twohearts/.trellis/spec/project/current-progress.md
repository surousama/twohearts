# 双心印当前进度

## 当前已完成

1. 角色基础移动已完成
2. `GAS` 基础底座已接入
3. 普通攻击已完成从 Character 级过渡逻辑到 `AbilitySystem` 的正式承载迁移
4. 普攻 `1-2-3` 已由 `Gameplay Ability` 驱动
5. 普攻阶段通知、最小 `Dodge` 打断、Guard 规则结算、玩家侧最小受击 / 伤害链路与预输入二期都已跑通

## 当前主线阶段

1. 当前仍处于第二章“基础战斗模块”推进阶段
2. 当前正确顺序是：
   普通攻击正式命中派发基础
   -> 敌方受击与生命最小闭环
   -> 玩家受击表现正式化
   -> 不可格挡与 GuardBreak 规则实现
   -> 完整格挡表现实现
   -> 战斗属性真相源收口
   -> 攻防样本扩展与回归验收

## 当前技术路线

1. 沿用 `UE5 + GAS + C++`
2. 核心逻辑在 `C++`
3. 表现层与资源配置在蓝图/数据

## 对 AI 的要求

1. 不要跳过当前顺序提前做后面的底座
2. 不要为了局部需求回退到 Character 级临时状态机
3. 如果建议改变顺序，必须明确理由并指出当前顺序的风险
4. 不要再拿“Guard 尚未开始”“预输入尚未完成”的旧口径作为当前事实

## 迁移说明

当前内容由旧文档 `../docs/a双心印开发进度简介.md` 提炼。迁移验证完成前，旧文档继续保留用于回退和比对。
